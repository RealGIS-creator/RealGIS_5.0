import { OnlyDecimal3IntDirectiveDirective } from './only-decimal3-int-directive.directive';

describe('OnlyDecimal3IntDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyDecimal3IntDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
