import { ExactLengthThreeDirective } from './exact-length-three.directive';

describe('ExactLengthThreeDirective', () => {
  it('should create an instance', () => {
    const directive = new ExactLengthThreeDirective();
    expect(directive).toBeTruthy();
  });
});
