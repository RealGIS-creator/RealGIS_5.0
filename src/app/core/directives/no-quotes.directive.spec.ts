import { NoQuotesDirective } from './no-quotes.directive';

describe('NoQuotesDirective', () => {
  it('should create an instance', () => {
    const directive = new NoQuotesDirective();
    expect(directive).toBeTruthy();
  });
});
