import { ExactLengthTenDirective } from './exact-length-ten.directive';

describe('ExactLengthTenDirective', () => {
  it('should create an instance', () => {
    const directive = new ExactLengthTenDirective();
    expect(directive).toBeTruthy();
  });
});
