import { OnlyAlphanumericDashDirective } from './only-alphanumeric-dash.directive';

describe('OnlyAlphanumericDashDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyAlphanumericDashDirective();
    expect(directive).toBeTruthy();
  });
});
