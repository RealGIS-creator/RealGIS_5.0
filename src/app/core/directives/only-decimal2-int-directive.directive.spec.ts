import { OnlyDecimal2IntDirectiveDirective } from './only-decimal2-int-directive.directive';

describe('OnlyDecimal2IntDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyDecimal2IntDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
