import { OnlyDecimalCommaDirective } from './only-decimal-comma.directive';

describe('OnlyDecimalCommaDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyDecimalCommaDirective();
    expect(directive).toBeTruthy();
  });
});
